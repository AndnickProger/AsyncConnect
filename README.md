# AsyncConnect
Asynchronous C++ client for the CBR courses (cbr.ru) based on self-written components: network layer (TCPAsyncSocket, EndpointIPv4), event loop (IOContext/epoll), parsing. Linux, HTTP, C++17 + Boost. Console output.
